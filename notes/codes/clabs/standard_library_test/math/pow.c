/*
    pow(x, y)
    计算x的y次幂。
*/

#include <stdio.h>
#include <math.h>

int main(int argc, char const *argv[])
{
    printf("pow(1.5, 2) = %.2f\n", pow(1.5, 2));          // 2.25
    
    return 0;
}
