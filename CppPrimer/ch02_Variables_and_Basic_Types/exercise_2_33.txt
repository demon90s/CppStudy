练习2.33：利用本节定义的变量，判断下列语句的运行结果。

a = 42; b = 42; c = 42;
d = 42; e = 42; g = 42;

---

a = 42; 结果是变量a的值变为42。
b = 42; 结果是变量b的值变为42。
c = 42; 结果是变量c的值变为42。
d = 42; 非法的赋值，不能用整型值给指针赋值。
e = 42; 非法的赋值，不能用整型值给指针赋值。
g = 42; 非法的赋值，g是一个常量引用，不能修改其值。
