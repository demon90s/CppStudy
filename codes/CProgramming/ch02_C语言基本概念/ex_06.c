/*
 * 编写一个程序用来声明几个 int 型和 float 型变量，不对这些变量进行初始化，然后打印出它们的值。
 * 这些值是否有规律？（通常情况下没有）
 */

/*
 * 没有规律。
 */

#include <stdio.h>

int main()
{
	int i, j, k;
	float x, y;

	printf("i: %d\n", i);
	printf("j: %d\n", j);
	printf("k: %d\n", k);

	printf("x: %f\n", x);
	printf("y: %f\n", y);

	return 0;
}
