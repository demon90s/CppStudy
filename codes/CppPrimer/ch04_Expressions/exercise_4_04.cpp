/*
 * 练习4.4：在下面的表达式中添加括号，说明其求值的过程及最终结果。编写程序编译该（不加括号的）表达式并输出其结果验证之前的推断。
 *
 * 12 / 3 * 4 + 5 * 15 + 24 % 4 / 2
 */

/*
 * ((12 / 3) * 4) + (5 * 15) + (24 % (4 / 2))
 *
 * 结果是：16 + 75 + 0 = 91
 */

#include <iostream>

int main()
{
	std::cout << 12 / 3 * 4 + 5 * 15 + 24 % 4 / 2 << std::endl;

	return 0;
}
